import { useState } from "react";
import { Button } from "chip-ui";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return (
    <>
      <Button></Button>
    </>
  );
}

export default App;
